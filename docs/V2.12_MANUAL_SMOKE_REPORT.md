# V2.12 Manual Required Smoke Report

> 基于 v2.12.0 tag (0553edb) 的 35 项 manual required 手工验证报告。
> 本报告已补充 assistant 通过 Obsidian CLI + Chromium Remote Debugging（CDP）执行的真实运行时验证结果。

## 0. 重要说明

本轮结果分两层：

- ✅ 已完成：35 项代码级证据收集
- ✅ 已完成：通过 Obsidian CLI + CDP 的真实运行时验证（可脚本化部分）
- ⏭️ 仍有少量项无法可靠脚本化，需要纯目视或人工交互确认

注意：

- 本轮实际连接到的运行中 Vault 为 `D:\Users\Ye_Luo\APP\Obsidian\LLM-Wiki`
- 文档原始目标 Vault `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki` 在当前 Obsidian 启动链路下未成为实际活动 Vault

**当前结论**：发现 1 个 blocker：`Skill` 重命名后 `meta`（`pinned/applyCount/lastUsedAt`）未迁移到新名称。V2.12.0 不应继续维持“无 blocker”结论。

## 1. Tag 锁定状态

| Tag | Commit | 状态 |
|---|---|---|
| v2.12.0 | 0553edbb604f39be9fb4946655ff93a930049176 | ✅ 锁定 |
| v2.11.1 | 9b0c913bdae0077a7398b2f6417a444f851981a4 | ✅ 锁定未移动 |

## 2. 35 项 Manual Required 验证清单

### 2.1 核心用户流（1-7）

| ManualId | 场景 | 代码级证据 | 真实 UI 结果 | Blocker |
|---|---|---|---|---|
| 1 | 自由提问 | view.ts:151 inputEl 存在；view.ts:372 keydown 监听 Ctrl+Enter 发送 | ✅ 通过 — `auto/claude` 实测完成，assistant 返回 `OK`，状态到 `Done` | 否 |
| 2 | 解释选区 | view.ts:18 import PRESETS；presetPrompts.ts 含 Selection preset；view.ts:334 渲染 preset 按钮 | ⏭️ 跳过 — CDP 下 Bridge View 获焦后无法稳定保留编辑器选区，不足以可靠判定 | 否 |
| 3 | 总结当前笔记 | presetPrompts.ts 含 Note preset；view.ts:1268 openGeneratedFile 点击打开 | ✅ 通过 — 切到 `acceptEdits` 后 real run 成功生成 `90_AI整理待确认/AGENTS-summary.md` | 否 |
| 4 | 应用单个 Skill | view.ts:2324 comboCheck；skills.ts applySkill 注入 prompt；{{outputDir}} 替换 | ✅ 通过 — `改写润色` 注入成功，输入框含 `replace_selection` 指令 | 否 |
| 5 | 组合应用多个 Skill | view.ts:117 skillsComboSet Set 按插入顺序；view.ts applyCombo for...of skillsComboSet（V2.11.1 修复） | ✅ 通过 — 组合应用后 prompt 按勾选顺序拼接 | 否 |
| 6 | 生成/修改 Markdown | view.ts:1268 openGeneratedFile 点击；fileDiff.ts 检测新增/修改 | ✅ 通过 — summary run 后 `generatedFiles` 显示 `AGENTS-summary.md [NEW]` | 否 |
| 7 | 恢复历史会话 | view.ts:2000 restoreSession 点击；view.ts:2048 restoreSession 实现 | ✅ 通过 — 从 History 恢复成功，消息与会话标题恢复 | 否 |

### 2.2 Skills 验证（8-14）

| ManualId | 场景 | 代码级证据 | 真实 UI 结果 | Blocker |
|---|---|---|---|---|
| 8 | 搜索 | view.ts skillsSearchEl + skillsSearchDebounceTimer（300ms 防抖） | ✅ 通过 — 导入带标签测试 skill 后，搜索 `标签` 仅返回该 skill | 否 |
| 9 | 标签分组 | view.ts skillsGroupEl + skillsSortEl 分组下拉 | ✅ 通过 — 选择 `测试` 分组后仅显示带 `#测试` 的 skill | 否 |
| 10 | 置顶 | view.ts pinBtn + isPinned；skillsState.ts pinned 字段持久化 | ✅ 通过 — `改写润色` 置顶后 `pinned=true`，列表排到最前 | 否 |
| 11 | 使用统计 | view.ts applyCount + lastUsedAt；skillsState.ts 字段定义 | ✅ 通过 — 应用后 `applyCount=1` 且 `lastUsedAt` 写入 | 否 |
| 12 | tags 编辑保留 | skills.ts updateImportedSkill extractTags（V2.11.1 修复）；view.ts EditSkillModal 预填 #标签 | ✅ 通过 — 编辑描述后 tags 保留并更新为 `#测试 #分组 #保留` | 否 |
| 13 | 重命名 meta 保留 | skillsState.ts renameSkillMeta（V2.11.1 修复）；view.ts openEditSkillDialog 调用 | ❌ 失败 — 复现：给导入 skill 先 `pin + apply` 后改名；结果旧名仍保留 `meta`，新名 `meta=null`，迁移未生效 | 是 |
| 14 | 组合应用顺序 | view.ts:117 skillsComboSet Set 插入顺序；applyCombo for...of（V2.11.1 修复） | ✅ 通过 — 实测顺序为 `总结当前笔记 → 改写润色 → 解释选区` | 否 |

### 2.3 Session 验证（15-20）

| ManualId | 场景 | 代码级证据 | 真实 UI 结果 | Blocker |
|---|---|---|---|---|
| 15 | 自动保存 | sessions.ts:132 saveSession 原子写（tmp+rename）；view.ts 运行后触发 | ✅ 通过 — `.llm-bridge/sessions/` 已新增 session json，含消息与 trace | 否 |
| 16 | 历史搜索 | view.ts historySearchEl + historySearchDebounceTimer（300ms 防抖） | ✅ 通过 — 搜索 `OK` / `自动化` 均按标题正确过滤 | 否 |
| 17 | 标题重命名 | sessions.ts:281 renameSession 原子写；view.ts 调用 | ✅ 通过 — 历史会话从 `请只回复 OK` 成功改名为 `自动化会话OK` | 否 |
| 18 | 恢复完整性 | view.ts:2048 restoreSession 恢复 messages/状态/生成文件/workflow trace | ✅ 通过 — 恢复后 `messageCount=6`，标题与当前 session id 同步 | 否 |
| 19 | 删除确认 | sessions.ts:267 deleteSession；view.ts 删除按钮 + 确认对话框 | ✅ 通过 — 删除确认后会话从 History 消失 | 否 |
| 20 | session 文件无 secret | sessions.ts:73 redactSessionMessages + redactSdkEventForSession（V2.11.1 defense-in-depth） | ✅ 通过 — 最新 session 文件未检出 token/API key/Authorization 明文 | 否 |

### 2.4 权限和停止（21-24）

| ManualId | 场景 | 代码级证据 | 真实 UI 结果 | Blocker |
|---|---|---|---|---|
| 21 | low 自动允许 | permissionPolicy.ts:154 low + (policy!=high) → auto_allow | ✅ 通过 — 集成测试中 `open_note/get_selection/get_active_note` 低风险路径无需审批 | 否 |
| 22 | medium 不静默 | permissionPolicy.ts:155 medium 风险需本轮授权 needs_approval | ✅ 通过 — 集成测试中 `create_note/append_to_note` 返回 `pending_approval` | 否 |
| 23 | high 不静默 | permissionPolicy.ts:155 high → needs_approval（始终） | ⏭️ 跳过 — 当前 action 集合无稳定可重放的高风险 UI 场景，不想伪造非产品路径 | 否 |
| 24 | stop 不挂死 | view.ts:688 onClose runHandle.stop()；view.ts:700 清理 skillsSearchDebounceTimer；view.ts:692 清理 scrollRafId | ⏭️ 跳过 — 需构造“运行中 + pending permission + Stop”三者同时存在的交互态，当前 CLI harness 未覆盖 | 否 |

### 2.5 UI 验证（25-31）

| ManualId | 场景 | 代码级证据 | 真实 UI 结果 | Blocker |
|---|---|---|---|---|
| 25 | 首屏不拥挤 | view.ts createCollapsibleSection startOpen=false；Skills/History/Advanced 默认折叠 | ✅ 通过 — 首屏为输入区+状态栏，Skills/History/Advanced 默认 `hidden` | 否 |
| 26 | Skills 默认折叠 | view.ts renderSkillsPanel body setAttribute hidden | ✅ 通过 — `.llm-bridge-skills-body[hidden]` 初始存在 | 否 |
| 27 | History 默认折叠 | view.ts renderHistoryPanel body setAttribute hidden | ✅ 通过 — `.llm-bridge-history-body[hidden]` 初始存在 | 否 |
| 28 | Advanced 默认折叠 | view.ts sbAdvancedItems setAttribute hidden | ✅ 通过 — `.llm-bridge-sb-advanced-items[hidden]` 初始存在 | 否 |
| 29 | chip 切换稳定 | view.ts workspace.on("file-open") 订阅（V2.10 B-001 修复） | ⏭️ 跳过 — file chip 已验证会随活动笔记切换，但 selection chip 在 CDP 焦点切换下无法可靠判定 | 否 |
| 30 | timeline tooltip | view.ts llm-bridge-timeline-detail attr title=entry.detail | ⏭️ 跳过 — 当前运行态未稳定保留可见 timeline detail 节点，未强行造假 | 否 |
| 31 | workflow trace tooltip | view.ts llm-bridge-workflow-trace-detail attr title=entry.detail | ✅ 通过 — DOM 中 `workflow trace detail` 均带 `title`，如 `984 chars via stdin` | 否 |

### 2.6 错误体验（32-34）

| ManualId | 场景 | 代码级证据 | 真实 UI 结果 | Blocker |
|---|---|---|---|---|
| 32 | 错误摘要可读 | view.ts 错误展示含 exitCode + redactSecrets 脱敏 | ✅ 通过 — `mock-failure` 下错误摘要含 `exit 1`，无 secret 明文 | 否 |
| 33 | debug log 路径 | view.ts clipboard.writeText(logPath) 可复制 | ✅ 通过 — 失败态 stderr 含具体 log 路径，UI 存在 `复制路径` 按钮 | 否 |
| 34 | 文件丢失完整路径+复制 | view.ts:3173 showFileNotFoundModal + clipboard.writeText(relPath) | ⏭️ 跳过 — 本轮消息流在后续 run 后被覆盖，未稳定复现到缺失文件弹窗 | 否 |

### 2.7 敏感信息扫描（35）

| ManualId | 场景 | 代码级证据 | 真实 UI 结果 | Blocker |
|---|---|---|---|---|
| 35 | session 文件真实扫描 | sessions.ts:73-88 redactSessionMessages 脱敏 content/stderr/log/timeline/commandPreview/workflowTrace/sdkEvents | ✅ 通过 — 最新 session JSON 已实扫，未见 token/API key/Authorization 明文 | 否 |

## 3. 代码级证据汇总

| 类别 | 项数 | 代码级通过 | 说明 |
|---|---|---|---|
| 核心用户流 | 7 | 7/7 ✅ | inputEl/PRESETS/openGeneratedFile/applySkill/skillsComboSet/restoreSession |
| Skills | 7 | 7/7 ✅ | 搜索/分组/置顶/统计/extractTags/renameSkillMeta/Set 顺序 |
| Session | 6 | 6/6 ✅ | saveSession/搜索/renameSession/restoreSession/deleteSession/redactSessionMessages |
| 权限和停止 | 4 | 4/4 ✅ | auto_allow/needs_approval/onClose stop/定时器清理 |
| UI | 7 | 7/7 ✅ | createCollapsibleSection/hidden/file-open/attr title |
| 错误体验 | 3 | 3/3 ✅ | redactSecrets/clipboard.writeText/showFileNotFoundModal |
| 敏感信息 | 1 | 1/1 ✅ | redactSessionMessages + redactSdkEventForSession |
| **合计** | **35** | **35/35 ✅** | **代码级证据全部通过** |

## 4. 自动化测试结果（V2.12.0 Post-Release Smoke）

| 测试 | 结果 |
|---|---|
| build | ✅ 通过 |
| test:unit | 562 passed, 0 failed |
| test:process | 62 passed, 0 failed |
| test:claude | 55 passed, 0 failed |
| V2.12 Long Flow E2E 段 | 38 个代码级断言全绿 |

## 5. 最终结论

### 当前状态（代码级 + 运行时）

**发现 blocker：Skill 重命名后 meta 未迁移。**

- 代码级证据：35/35 通过
- 运行时/CDP：大部分可脚本化项已实测，主链路可跑通
- blocker：`ManualId 13` 失败，`renameSkillMeta` 预期行为在真实编辑流里未体现
- 自动化测试：build + unit 562 + process 62 + claude 55 全绿，但未覆盖该真实交互缺陷

### 仍建议补的纯人工项

以下项本轮保留 `⏭️`，如要 UI 微调前再收紧质量门槛，建议人工补看：

1. `ManualId 2` 解释选区
2. `ManualId 23` high 风险权限
3. `ManualId 24` stop + pending permission
4. `ManualId 29` selection chip 实时性
5. `ManualId 30` timeline tooltip
6. `ManualId 34` 文件丢失弹窗

### V2.12.1 Patch 触发条件

**仅在以下情况触发 V2.12.1 patch**：
- 用户手工验证发现 blocker（❌ 失败项）
- 非阻断问题记录为 follow-up，不 bump 版本

### Recommendation

**不建议维持 “No Blocker / Release Stable” 原结论。** 在进入 UI 调整前，应先修复 `Skill` 重命名后的 `meta` 迁移缺陷，再回归验证相关项。
