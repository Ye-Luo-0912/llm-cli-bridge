# V2.15-D - Final Release Candidate / Version Bump / Docs Freeze Report

## VersionDecision

Baseline requested and confirmed: `1996a1c feat: compact plugin shell rail`.

Version was bumped for the V2.15 release candidate:

- `package.json`: `2.12.1` -> `2.15.0`
- `manifest.json`: `2.12.1` -> `2.15.0`
- Runtime bridge after Obsidian restart reports `pluginVersion=2.15.0`

No tag was created in this phase.

## ReleaseArtifact

Repository build artifact:

| File | Size | MTime | SHA256 |
| --- | ---: | --- | --- |
| `main.js` | 415771 | `2026-06-30T22:35:21.3269638+08:00` | `650F719EEC3362E1DB2CA74DB797EF66614D2E3067A9E0D880B4395D716B612B` |
| `manifest.json` | 289 | `2026-06-30T22:28:37.8479392+08:00` | `66E95F3288EEC96C8A5EE16D822B0A60F9F8F7BF220119D541177DE287944265` |
| `styles.css` | 69815 | `2026-06-30T22:09:16.7168239+08:00` | `4911E05941D898D0A7F7F863CF9C990F86B692885C7B3B3E81F0139401318AB8` |

Release zip:

| File | Size | MTime | SHA256 |
| --- | ---: | --- | --- |
| `release/llm-cli-bridge-2.15.0.zip` | 120034 | `2026-06-30T22:35:22.3075578+08:00` | `6FD7DDAB8CDAD9A464A031CB75AF9DB4B91272A60ADBAF9D37A233CC50694E1C` |

`npm run release` completed and staged exactly 6 files:

- `main.js`
- `manifest.json`
- `styles.css`
- `README.md`
- `RELEASE_CHECKLIST.md`
- `USER_GUIDE.md`

Release stage sensitive scan passed: scanned 6 files, no sensitive information found.

Test Vault artifact target:

- `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki\.obsidian\plugins\llm-cli-bridge`

The test Vault was refreshed with the current `main.js`, `manifest.json`, and `styles.css`.

## DocsFreeze

Updated documentation:

- `README.md`
- `docs/USER_GUIDE.md`
- `RELEASE_CHECKLIST.md`

Frozen documentation points:

- Install/upgrade uses the 6-file release zip copy workflow.
- Claude runtime config uses only `CLAUDE_CONFIG_DIR`.
- `ANTHROPIC_CONFIG_DIR` is explicitly documented as not used.
- Project-level config is documented through `<Vault>/.llm-bridge/claude-runtime.json`.
- Native handoff boundary is documented: Claude Code / SDK execute native Vault file operations; the plugin handles context, Working Set, attachments, permissions, and UI.
- UI structure is documented as `Chat / Files / Skills / History`.
- Known limitations are documented: no self-built write executor, no backup/rollback/audit transaction, no OCR/PDF/image parser, no SDK image streaming, no external writes.

## FinalSmoke

Obsidian runtime:

| Check | Result | Evidence |
| --- | --- | --- |
| Bridge online | Pass | `bridge.json` present, port `63559`, pluginVersion `2.15.0`. |
| `GET /state` | Pass | HTTP 200 and `ok=true`. |
| `show_notice` | Pass | HTTP 200 and action completed. |
| `create_note` approval flow | Pass | Initial action returned `pending_approval`; `/dev/approve` with `id` returned 200; note created under `90_AI整理待确认`. |
| `append_to_note` approval flow | Pass | Initial action returned `pending_approval`; `/dev/approve` returned 200; appended content was present after a short filesystem wait. |

Real Obsidian UI/CDP smoke:

| Check | Result | Evidence |
| --- | --- | --- |
| Compact rail | Pass | Rail width `44px`; 4 nav items only. |
| No left Settings/brand/collapse | Pass | `.llm-bridge-nav-label`, left brand, left collapse, and left Settings were absent. |
| Chat / Files / Skills / History switching | Pass | All four page DOM nodes existed, became visible, and active nav synchronized. |
| Topbar | Pass | Topbar text included `Bridge`; runtime text `Claude Code · 已连接`. |
| Composer command menu | Pass | `.llm-bridge-command-menu` present. |
| Permission chip | Pass | Text `权限：默认`. |
| Working Set strip | Pass | Strip present with `Note`, `AGENTS.md`, and `Selection` chips/status text. |

Claude Code native smoke:

| Check | Result | Evidence |
| --- | --- | --- |
| `where claude` | Pass | `C:\Users\Ye_Luo\AppData\Local\fnm_multishells\22448_1782830006746\claude(.cmd)`. |
| Claude version | Pass | `2.1.196 (Claude Code)`. |
| Runtime config | Pass | `CLAUDE_CONFIG_DIR=D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-AgentRuntime\private\claude-config`; `ANTHROPIC_CONFIG_DIR=<unset>`. |
| Minimal prompt | Pass | `claude -p --output-format json` returned `v215d-cli-ok`. |
| Native Vault read/edit | Pass | Claude read Vault context and created `90_AI整理待确认/V2.15-D-claude-native-edit-1782830029636.md` containing `claude-native-edit-ok-1782830029636`. |

SDK smoke:

| Check | Result | Evidence |
| --- | --- | --- |
| SDK package | Pass | Imported portable `@anthropic-ai/claude-agent-sdk@0.3.191` from `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-AgentRuntime\node_modules`. |
| Runtime config | Pass | `CLAUDE_CONFIG_DIR` set to the project-local runtime config; `ANTHROPIC_CONFIG_DIR=<unset>`. |
| Minimal prompt | Pass | SDK `query()` returned `v215d-sdk-ok` with `settingSources: ["user", "project", "local"]` and `cwd` set to the test Vault. |

## BoundaryRegression

External and sensitive write boundaries:

| Check | Result | Evidence |
| --- | --- | --- |
| External absolute write deny | Pass | `create_note` to `D:/Users/Ye_Luo/APP/Test/outside-v215d-smoke.md` returned HTTP 400 with `拒绝绝对路径`. |
| Sensitive `.env` write deny | Pass | `create_note` to `.env` returned HTTP 400 with `拒绝写入敏感路径`. |

Runtime boundary diff:

- `src/promptPackage.ts`: unchanged.
- `src/claudeCliBackend.ts`: unchanged.
- `src/sdkBackend.ts`: unchanged.
- `src/runtimeFileToolAdapter.ts`: unchanged.
- `src/fileToolExecutor.ts`: unchanged.
- `src/agentBackend.ts`: unchanged.

No self-built write executor, backup/rollback/audit transaction, PDF/OCR/image parser, SDK image streaming, or complex runtime tool registration was added.

## Tests

| Check | Result | Evidence |
| --- | --- | --- |
| `npm run build` | Pass | TypeScript and esbuild completed under package version `2.15.0`. |
| `npm run test:unit` | Pass | `650 passed`, `0 failed`, `25 skipped`, `0 manual required`. |
| `npm run test:process` | Pass | `62 passed`, `0 failed`, `53 skipped`, `0 manual required`. |
| `npm run release` | Pass | Build, 6-file stage validation, sensitive scan, and zip packaging completed. |

`docs/test-report.md` was regenerated and now reports plugin version `2.15.0`.

## RemainingRisk

- Smoke-created notes remain in the test Vault under `90_AI整理待确认` as evidence artifacts.
- The append approval smoke needs a short filesystem wait before reading the file immediately after `/dev/approve`; the action itself completed and content was present after waiting.
- The release checklist history row uses `pending` for the V2.15.0 RC commit because the final commit hash is produced after this report is committed.
- Same-name Vault ambiguity remains an operational risk if Obsidian is launched without the explicit portable user data path.

## Recommendation

V2.15 is release-candidate ready from this freeze pass.

Release candidate basis:

- Version metadata is synchronized to `2.15.0`.
- Release zip builds successfully and scans clean.
- Build, unit tests, and process tests pass.
- Real Obsidian compact shell smoke passes on the refreshed test Vault artifact.
- Claude Code native minimal/read/edit passes with project-local `CLAUDE_CONFIG_DIR`.
- SDK minimal prompt passes with the same config boundary.
- External writes and sensitive `.env` writes remain denied.
- Runtime/file execution responsibilities did not expand beyond the V2.14 native handoff boundary.
