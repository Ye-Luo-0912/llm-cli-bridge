# V2.14.0-O - Release Readiness / Final Smoke & Fixes Report

## ReleaseArtifact

Baseline: `ad1ddd3 fix: remove anthropic config dir handoff`.

Unique test Vault:

- `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki`

Plugin artifact target:

- `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki\.obsidian\plugins\llm-cli-bridge`

Artifact freshness:

| File | Size | MTime | SHA256 |
| --- | ---: | --- | --- |
| `main.js` | 407454 | `2026-06-30T20:14:40.6690866+08:00` | `D01426B3E7F0367325DCBD5CF2BCBCB5B532B02C62008AE3C2D0CF35972EA412` |
| `manifest.json` | 290 | `2026-06-29T18:40:06.7656846+08:00` | `459E6FCFED635B430E03A6CB21E0279F1A703E4894FE4FD7AFA139381007067F` |
| `styles.css` | 59370 | `2026-06-30T18:36:43.8671879+08:00` | `B7735FD71B870655FC5E4B086C02D5DB877C5AD846D4B12856FFDAF6D02DDA9B` |

Manifest version:

- `2.12.1`

Obsidian was restarted with the explicit portable user data path:

- `--user-data-dir=D:\Users\Ye_Luo\APP\Test\Obsidian\Data\Roaming\Obsidian`

The first launch attempt without this explicit user data directory opened a same-name global profile. That was treated as an operational blocker and corrected before smoke evidence was accepted.

## TestResults

| Check | Result | Evidence |
| --- | --- | --- |
| `npm run build` | Pass | TypeScript and esbuild completed. |
| `npm run test:unit` | Pass | `647 passed`, `0 failed`, `25 skipped`, `0 manual required`. |
| `npm run test:process` | Pass | `62 passed`, `0 failed`, `53 skipped`, `0 manual required`. |

No runtime feature expansion was made during this phase.

## BridgeSmoke

Bridge file:

- `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki\.llm-bridge\bridge.json`

Bridge facts:

| Field | Value |
| --- | --- |
| Port | `49720` |
| StartedAt | `2026-06-30T12:27:20.939Z` |
| VaultPath | `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki` |
| PluginVersion | `2.12.1` |

Smoke matrix:

| Check | Result | Evidence |
| --- | --- | --- |
| Bridge online | Pass | `bridge.json` present and HTTP port connected. |
| `GET /state` | Pass | Returned `status=completed`, `result.vaultPath=D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki`, `activeFilePath=AGENTS.md`. |
| `show_notice` | Pass | `/action` completed with status 200. |
| `create_note` approval flow | Pass | Initial request returned `pending_approval`; `/dev/approve` completed; note created under `90_AI整理待确认`. |
| `append_to_note` approval flow | Pass | Initial request returned `pending_approval`; `/dev/approve` completed; appended content was present. |

## ClaudeCodeSmoke

Runtime config source:

- Project file: `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki\.llm-bridge\claude-runtime.json`
- Resolved `CLAUDE_CONFIG_DIR`: `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-AgentRuntime\private\claude-config`
- `ANTHROPIC_CONFIG_DIR`: not set and not used.

Smoke matrix:

| Check | Result | Evidence |
| --- | --- | --- |
| Minimal prompt | Pass | `claude -p --output-format json "Reply with exactly: o-cli-ok"` returned `o-cli-ok`. |
| Native Vault read | Pass | Claude Code read `AGENTS.md` from the Vault cwd. |
| Native Vault edit | Pass | Claude Code created `90_AI整理待确认/V2.14.0-O-claude-native-edit-1782823097451.md`. |

This path used Claude Code native file capability. No plugin-owned write executor, backup transaction, rollback mechanism, audit transaction, PDF parser, OCR, SDK image streaming, or complex runtime tool registration was added.

## SdkSmoke

SDK package source:

- `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-AgentRuntime\node_modules\@anthropic-ai\claude-agent-sdk`
- Observed package version: `0.3.191`
- Observed Claude Code native version in SDK init: `2.1.191`

Runtime config source:

- Resolved `CLAUDE_CONFIG_DIR`: `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-AgentRuntime\private\claude-config`
- `ANTHROPIC_CONFIG_DIR`: not set and not used.
- SDK options used `cwd` set to the test Vault and `settingSources: ["user", "project", "local"]`.

Smoke matrix:

| Check | Result | Evidence |
| --- | --- | --- |
| SDK load | Pass | SDK imported from the portable `LLM-AgentRuntime` package. |
| Minimal prompt | Pass | SDK `query()` returned `o-sdk-ok`. |
| Native Vault read | Pass | SDK route read `AGENTS.md` from the Vault cwd. |
| Native Vault edit | Pass | SDK route created `90_AI整理待确认/V2.14.0-O-sdk-native-edit-1782823341683.md`. |

One combined SDK minimal+edit attempt hit `Connection closed mid-response`; it was rerun as separate calls and passed. This is recorded as transient runtime/network behavior, not a plugin blocker.

## ExternalBoundary

| Check | Result | Evidence |
| --- | --- | --- |
| External absolute write deny | Pass | `create_note` to `D:\Users\Ye_Luo\APP\Test\outside-release-smoke.md` returned HTTP 400 with `拒绝绝对路径`. |
| Runtime adapter write expansion | Pass | No write/delete/rename adapter route was added in this phase. |

External write/delete/rename remains closed.

## SensitiveBoundary

| Check | Result | Evidence |
| --- | --- | --- |
| `.env` write deny | Pass | `create_note` to `.env` returned HTTP 400 with `拒绝写入敏感路径`. |
| Sensitive runtime behavior | Pass | No code path was added that bypasses the existing sensitive path policy. |

Sensitive paths remain protected by the existing deny/strong-risk policy boundary.

## Fixes

No runtime code changes were required in V2.14.0-O.

Operational fixes performed during smoke:

- Refreshed the plugin artifact in the unique test Vault.
- Restarted Obsidian with the explicit portable user data path after detecting a same-name global profile launch.
- Corrected the Claude Code smoke working directory to the test Vault so native read/edit evidence applies to the intended Vault.
- Removed the accidental repo-local smoke artifact created by the first cwd-misconfigured CLI attempt.

## RemainingRisk

- Obsidian launched without the explicit portable `--user-data-dir` can still open the wrong same-name profile on this machine. Release smoke should continue to launch the portable profile explicitly.
- The SDK combined minimal+edit run once failed with `Connection closed mid-response`, but isolated minimal and read/edit SDK smokes passed.
- Smoke-created notes remain in the test Vault under `90_AI整理待确认` as evidence artifacts.

## Recommendation

V2.14 current mainline is release-ready from this final smoke pass.

Release readiness basis:

- Build, unit tests, and process tests pass.
- The refreshed artifact runs in the unique test Vault.
- Bridge HTTP and approval flows pass.
- External absolute writes and sensitive `.env` writes are denied.
- Claude Code native minimal/read/edit passes through project-local `CLAUDE_CONFIG_DIR`.
- SDK native minimal/read/edit passes through the same project-local runtime config.
- No self-built write executor, backup/rollback/audit transaction, OCR/PDF/image parser, SDK image streaming, or complex runtime tool registration was introduced.
