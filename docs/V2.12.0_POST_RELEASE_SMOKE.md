# V2.12.0 Post-Release Smoke Report

> 基于 v2.12.0 tag (0553edb) clean checkout 的发布后验证。

## 1. 测试环境

| 项目 | 值 |
|---|---|
| 验证时间 | 2026-06-29 |
| Tag | v2.12.0 |
| Release Commit | 0553edb (polish: V2.12 RELEASE_CHECKLIST 补 commit hash) |
| Checkout 方式 | git worktree add (detached HEAD 0553edb) |
| Node | v24.14.0 |
| Platform | win32 |
| Claude CLI | 2.1.195 (Claude Code) |

## 2. 版本链验证

| 检查项 | 预期 | 实际 | 状态 |
|---|---|---|---|
| manifest.json version | 2.12.0 | 2.12.0 | ✅ |
| package.json version | 2.12.0 | 2.12.0 | ✅ |
| HEAD commit | 0553edb | 0553edbb604f39be9fb4946655ff93a930049176 | ✅ |
| v2.11.1 tag | 9b0c913 | 9b0c913bdae0077a7398b2f6417a444f851981a4 | ✅ 未移动 |
| plugin id | llm-cli-bridge | llm-cli-bridge | ✅ |

**结论：版本链 5/5 一致，无 tag 漂移。**

## 3. Smoke 测试结果

### build
- `npm run build` — ✅ 通过（tsc -noEmit -skipLibCheck + esbuild production，obsidian-llm-cli-bridge@2.12.0）

### test:unit
- 结果：**562 passed, 0 failed, 22 skipped, 0 manual required**
- V2.12 Long Flow E2E / Real Daily Smoke 段：38 个代码级断言全绿
- 覆盖：约束确认 / UI 默认折叠 / tooltip / 权限策略 / stop 清理 / 错误体验 / Skills / Session / 核心流 / 报告输出

### test:process
- 结果：**62 passed, 0 failed, 46 skipped, 0 manual required**
- 覆盖：Process 启动/stdout/stderr/exit/stop/cwd；Preflight 真实探测（Claude 2.1.195）；File Diff

### test:claude
- 结果：**55 passed, 0 failed, 44 skipped, 0 manual required**
- 覆盖：Claude Smoke version 2.1.195 / started / stdout_delta / completed exitCode 0；Note Summarize 真实执行

## 4. Long Flow E2E / Real Daily Smoke 验证

V2.12 测试段（8.20）38 个代码级断言全部通过，覆盖：

| 类别 | 断言数 | 状态 |
|---|---|---|
| 约束确认（AgentEvent/CLI/sdk-experimental） | 3 | ✅ |
| UI 默认折叠（Skills/History/Advanced） | 4 | ✅ |
| tooltip（timeline/workflow/sdk event） | 3 | ✅ |
| 权限策略（low/high/medium） | 4 | ✅ |
| stop 清理（stopBtn/onClose/scrollRafId） | 3 | ✅ |
| 错误体验（showFileNotFoundModal/clipboard/log） | 3 | ✅ |
| Skills（搜索/分组/置顶/统计/重命名/组合） | 6 | ✅ |
| Session（搜索/重命名/删除/恢复/脱敏） | 5 | ✅ |
| 核心流（inputEl/Selection/Note/openGeneratedFile/preset） | 5 | ✅ |
| 报告输出（docs 目录/模板） | 2 | ✅ |

## 5. settings/skills/sessions 基础加载（代码级）

| 检查项 | 状态 |
|---|---|
| settings.ts 存在 + 关键配置 onChange 刷新 | ✅ |
| skills.ts 存在 + updateImportedSkill extractTags | ✅ |
| skillsState.ts 存在 + renameSkillMeta | ✅ |
| sessions.ts 存在 + redactSessionMessages defense-in-depth | ✅ |
| view.ts 存在 + inputEl/Skills/History 面板 | ✅ |
| types.ts 存在 + DEFAULT_SETTINGS backendMode="auto" | ✅ |

## 6. 约束确认

| 约束 | 状态 |
|---|---|
| 不改 AgentEvent v0.1 | ✅（agentBackend.ts 未修改，6 事件无 tool event） |
| 不改 CLI/auto 主线 | ✅（test:process 62 passed + test:claude 55 passed 真实执行） |
| sdk-experimental 默认关闭 | ✅（DEFAULT_SETTINGS.backendMode="auto"） |
| 不新增 tool event | ✅（workflowEvent.ts 未修改） |

## 7. 回滚路径

| 场景 | 回滚操作 |
|---|---|
| 回滚到 v2.11.1 | `git checkout v2.11.1` → 9b0c913 |
| 回滚到 v2.11.0 | `git checkout cf49292` |
| 回滚到 v2.10.0 | `git checkout e4f116c` |
| 紧急回滚（保留 V2.12 测试基础设施） | 禁用插件 → 重启 Obsidian → 启用旧版本 |

## 8. 结论

**Post-Release Smoke Passed.**

- 版本链：5/5 一致，无 tag 漂移
- Smoke 测试：build + unit (562) + process (62) + claude (55) 全绿，0 failed
- Long Flow E2E：38 个代码级断言全绿
- settings/skills/sessions 加载：6/6 代码级通过
- 约束：4/4 满足（AgentEvent/CLI/sdk-experimental/tool event）
- 回滚路径：4 种回滚方案可用

**无回归，无安装/启动阻断，无 long flow / daily smoke 阻断。**
