# V2.15-C - Compact Plugin Shell / Release Candidate Hardening Report

## CompactRail

Baseline: `1a6d611 feat: polish chat shell composer controls`.

The left navigation rail is now icon-only and compact:

- Default rail width is `44px`.
- Small viewport rail width is `40px`.
- The rail contains only Chat / Files / Skills / History icon buttons.
- Each icon button keeps `title` and `aria-label` for understandable hover/accessibility behavior.
- Active page remains visible through accent color, background, and the existing thin vertical active marker.

Removed from the left rail:

- Bridge brand text.
- Settings entry.
- Collapse button.
- Large text labels.

Live renderer evidence:

- Rail rect: `44px` wide.
- Rail text content: `☏▤◇◷`.
- Nav item count: 4.
- Nav label count: 0.
- Left rail brand element: absent.
- Left rail collapse element: absent.

## TopbarBrand

The `Bridge` brand moved to the topbar left side.

The compact topbar contains:

- Bridge logo/title.
- Current page label.
- Current session selector.
- `+ 新聊天`.
- Settings gear.
- Runtime status.

Live renderer evidence:

- Topbar text: `⌘BridgeChat当前会话新会话⌄+ 新聊天⚙Claude Code · 已连接`.
- Topbar brand rect: `121px` wide after flex hardening.
- Runtime status: `Claude Code · 已连接`.

## PageHeaders

Current page semantics are visible without widening the left rail:

- Chat is shown in the topbar page label.
- Files / Skills / History remain visible through their page body headers and the synchronized topbar page label.

Live page switch evidence:

- Chat page title: `Chat`.
- Files page title: `Files`.
- Skills page title: `Skills`.
- History page title: `History`.

## ComposerPreserved

The V2.15-B composer structure is preserved:

- Left: upload, command menu, permission mode chip.
- Middle: input box.
- Right: agent selector, model selector, effort selector, send/stop.

No composer runtime behavior was expanded.

Live renderer evidence:

- Composer rect: `431px` wide in the test pane, compared with `423px` before the compact rail.
- Composer still renders upload, command menu, permission chip, agent/model/effort, and send/stop.

## VisualSmoke

Test Vault: `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki`.

Plugin artifact refreshed into:

`D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki\.obsidian\plugins\llm-cli-bridge`

Artifact evidence:

| File | Size | SHA256 |
| --- | ---: | --- |
| `main.js` | `415771` | `650F719EEC3362E1DB2CA74DB797EF66614D2E3067A9E0D880B4395D716B612B` |
| `manifest.json` | `290` | `459E6FCFED635B430E03A6CB21E0279F1A703E4894FE4FD7AFA139381007067F` |
| `styles.css` | `69815` | `4911E05941D898D0A7F7F863CF9C990F86B692885C7B3B3E81F0139401318AB8` |

Bridge connectivity:

- `bridge.json` exists.
- Host `127.0.0.1`, port `64362`.
- `pluginVersion` reports `2.12.1`.

Real Obsidian renderer smoke passed:

- Compact rail width and icon-only content correct.
- Chat / Files / Skills / History switching works.
- Composer is not squeezed by the rail.
- Failed details still default collapsed: `▶ 查看详情`, `▶ debug log`.
- Collapsed debug log still contains copy/open controls.

## Tests

| Command | Result |
| --- | --- |
| `npm run build` | ✅ pass |
| `npm run test:unit` | ✅ 650 passed, 0 failed, 25 skipped |
| `npm run test:process` | ✅ 62 passed, 0 failed, 53 skipped |

## RemainingRisk

- Visual smoke used the live Obsidian Chromium renderer via debugging protocol.
- This stage does not change manifest `pluginVersion`; it remains inherited from the current baseline.

## Recommendation

V2.15-C is suitable to commit as release-candidate UI hardening. The shell now fits the Obsidian plugin pane better without expanding runtime responsibilities.
