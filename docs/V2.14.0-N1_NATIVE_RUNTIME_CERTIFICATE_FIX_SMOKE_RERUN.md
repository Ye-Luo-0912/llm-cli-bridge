# V2.14.0-N1 - Native Runtime Certificate Fix + CLI/SDK Smoke Rerun Report

## CertificateDiagnosis

Baseline commit: `9b2b8cc`.

Local command and runtime facts:

| Check | Result |
| --- | --- |
| `where claude` | `C:\Users\Ye_Luo\AppData\Local\fnm_multishells\11600_1782819365282\claude`, plus `.cmd` wrapper. |
| `claude --version` | `2.1.195 (Claude Code)` |
| Repo Node | `v22.22.2` |
| npm | `10.9.7` |
| `HTTPS_PROXY` / `HTTP_PROXY` / `NO_PROXY` | Initially unset. |
| Windows proxy | WinHTTP direct; user Internet Settings proxy disabled, stored server `127.0.0.1:7897`. |
| Local proxy process | Clash Verge / mihomo running; `127.0.0.1:7897` reachable. |

Claude user settings contain an endpoint override:

- `ANTHROPIC_BASE_URL=https://ark.cn-beijing.volces.com/api/coding`
- Auth token is present but redacted from diagnostics.

CA attempts:

- Default Claude Code loaded bundled and system CA stores.
- `NODE_USE_SYSTEM_CA=1` was tested.
- `NODE_EXTRA_CA_CERTS=D:\Users\Ye_Luo\APP\Test\llm-cli-bridge\.tmp-certs\windows-root-ca.pem` was tested after exporting Windows Root certificates to PEM.
- Debug logs showed `CA certs: Loaded 120 bundled root certificates`, `Loaded 109 system CA certificates`, and `Appended extra certificates from NODE_EXTRA_CA_CERTS`.

Network/TLS checks:

- Node HTTPS to `api.anthropic.com` succeeded with an authorized certificate.
- `curl --proxy http://127.0.0.1:7897 https://api.anthropic.com/` succeeded through the local proxy.
- Node HTTPS to `ark.cn-beijing.volces.com` failed with `ECONNRESET: Client network socket disconnected before secure TLS connection was established`.
- `curl --proxy http://127.0.0.1:7897 https://ark.cn-beijing.volces.com/api/coding/v1/messages` failed during TLS handshake after `HTTP/1.1 200 Connection established`.
- `curl -k` to the same Ark endpoint still failed during TLS handshake, so disabling certificate validation would not make this pass.

`NODE_TLS_REJECT_UNAUTHORIZED=0` was not used as pass evidence.

Conclusion: the blocker is not missing local CA roots. The current Ark endpoint path fails before certificate validation can complete, most likely due to endpoint/proxy/network routing. The previous `UNKNOWN_CERTIFICATE_VERIFICATION_ERROR` is now narrowed to the same native runtime connectivity class, not a plugin runtime defect.

## ClaudeCodeSmoke

Smoke command was rerun from the unique test Vault with:

- `NODE_USE_SYSTEM_CA=1`
- `NODE_EXTRA_CA_CERTS=<exported Windows Root PEM>`
- `HTTPS_PROXY=http://127.0.0.1:7897`
- `HTTP_PROXY=http://127.0.0.1:7897`
- `NO_PROXY=127.0.0.1,localhost`
- `claude -p --permission-mode acceptEdits --debug-file ... --output-format json ...`

Result: fail.

Evidence:

- Debug logs show repeated `API REQUEST /api/coding/v1/messages`.
- Debug logs show repeated `API error (attempt N/11): undefined Connection error`.
- The target native edit file was not created: `TARGET_EXISTS=False`.
- No plugin file write executor was involved.

The CLI native path has clear fail evidence and did not pass.

## SdkSmoke

SDK package availability:

- Repo-local `require('@anthropic-ai/claude-agent-sdk')`: not installed.
- Portable runtime package: `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-AgentRuntime\node_modules\@anthropic-ai\claude-agent-sdk`
- Portable SDK version: `0.3.191`
- Portable runtime Node: `v24.14.0`
- `@anthropic-ai/sdk` is also present in the portable runtime.

Minimal SDK `query()` was run through the portable runtime Node with the same proxy and CA environment.

Result: fail.

Evidence:

- SDK process timed out after 90 seconds.
- Residual processes included the portable `node.exe` and `claude-agent-sdk-win32-x64` native binary.
- The behavior matches the CLI path: native runtime starts, then stalls on the same endpoint connectivity class.

SDK did not pass. This is not treated as a skip.

## VaultSelection

The N-stage same-name Vault ambiguity was fixed operationally for this rerun by launching the portable test Vault directly instead of using an Obsidian URI.

Launch path:

- `D:\Users\Ye_Luo\APP\Test\Obsidian\Start-Obsidian-Portable.cmd`

Running bridge evidence:

- Vault: `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki`
- `pluginVersion` `2.12.1`
- Port: `61384`
- `bridge.json` regenerated under the test Vault.
- `diag-onload.txt` reports the same test Vault path and `bridgeWritten: true`.
- `GET /state` returned `ok: true`, active file `AGENTS.md`, and the same test Vault path.

This rerun used the intended test Vault, not the online same-name Vault.

## NativeReadEditResult

Native read/edit result: fail.

What was attempted:

- Read `AGENTS.md` from the test Vault.
- Create or update `90_AI整理待确认/V2.14.0-N1-claude-native-edit.md`.
- Use Claude Code native file capability, not plugin-owned write execution.

What happened:

- Claude Code did not reach tool execution.
- SDK did not reach tool execution.
- The edit file was not created.
- Plugin diff/result surface could not show native changes because there were no native changes to surface.

This is a real fail result for native read/edit under the current local endpoint configuration.

## RemainingRisk

- Release readiness remains blocked for native handoff until the Ark endpoint TLS/transport path is fixed or a working Anthropic/OAuth/API-key path is configured.
- Excluding user settings made Claude Code use default auth flow, but no OAuth/API-key credentials were available, so that path returned `Not logged in`.
- The local proxy works for `api.anthropic.com` but not for `ark.cn-beijing.volces.com`, so the fix likely belongs in Clash routing / Ark endpoint reachability / configured provider credentials rather than plugin code.
- SDK smoke depends on the portable runtime packages, not repo-local dependencies.

No plugin core feature was changed:

- No write executor.
- No backup/rollback/audit transaction.
- No PDF/OCR/image parser.
- No complex runtime tool registration.
- No `AgentEvent v0.1` change.

## Recommendation

Do not enter release readiness yet.

Fix the native provider path first:

1. Either repair the Ark endpoint TLS/transport route in Clash/provider configuration.
2. Or configure a working Claude Code login/API-key path that does not rely on the failing Ark endpoint.
3. Then rerun the same N1 CLI and SDK native read/edit smoke from `D:\Users\Ye_Luo\APP\Test\Obsidian\LLM-Wiki`.

The plugin can remain frozen as a native handoff bridge; the current blocker is local native runtime connectivity, not missing plugin functionality.
